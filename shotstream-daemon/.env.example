# docker-compose reads these. Rename to .env and adjust for your machine.

# Where the ShotStream weights + outputs live on the host.
# Windows example: D:\shotstream
# Linux example:   /mnt/data/shotstream
SHOTSTREAM_MODELS_DIR=D:/shotstream
