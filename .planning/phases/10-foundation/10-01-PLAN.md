---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/007_add_upscale_batches.sql
  - backend/models/upscale.py
  - backend/config/settings.py
  - backend/tests/test_upscale_models.py
autonomous: true
requirements: [INFR-01, INFR-02, SETT-01, SETT-02]

must_haves:
  truths:
    - "Database tables upscale_batches and upscale_videos exist with all columns for current and future phases"
    - "Pydantic models validate upscale settings with correct ranges and reject invalid values"
    - "Settings default to 2k resolution, creativity=0, sharpen=0, grain=0, fps_boost=off, vivid when omitted"
    - "FREEPIK_API_KEY and related config fields exist in Settings"
  artifacts:
    - path: "backend/migrations/007_add_upscale_batches.sql"
      provides: "upscale_batches and upscale_videos table creation with indexes"
      contains: "CREATE TABLE IF NOT EXISTS upscale_batches"
    - path: "backend/models/upscale.py"
      provides: "Pydantic models for batch/video/settings"
      exports: ["UpscaleSettings", "CreateBatchPayload", "BatchResponse", "UpscaleBatch", "UpscaleVideo", "BatchDetailResponse", "AddVideoPayload", "BatchStatus", "VideoStatus"]
    - path: "backend/config/settings.py"
      provides: "FREEPIK_API_KEY and related settings"
      contains: "FREEPIK_API_KEY"
    - path: "backend/tests/test_upscale_models.py"
      provides: "Tests for Pydantic model validation and defaults"
      min_lines: 60
  key_links:
    - from: "backend/models/upscale.py"
      to: "backend/migrations/007_add_upscale_batches.sql"
      via: "Column names and types match between Pydantic models and SQL schema"
      pattern: "resolution.*creativity.*sharpen.*grain.*fps_boost.*flavor"
---

<objective>
Create the database schema, Pydantic models, and settings configuration for batch video upscaling.

Purpose: Establish the data foundation that all subsequent plans depend on -- the tables, type contracts, and configuration that the Freepik service, job service, and API router will use.
Output: SQL migration file, Pydantic models file, updated settings, and model validation tests.
</objective>

<execution_context>
@/Users/yvesfogel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yvesfogel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-RESEARCH.md

@backend/config/settings.py
@backend/migrations/004_add_runpod_support.sql
@backend/models/video_job.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/config/settings.py:
```python
class Settings(BaseSettings):
    # RunPod pattern to follow:
    ENABLE_RUNPOD: bool = False
    RUNPOD_API_KEY: str = ""
    RUNPOD_ENDPOINT_ID: str = ""
    RUNPOD_TIMEOUT: int = 600
```

From backend/core/auth.py:
```python
def get_current_user(...):
    # Returns user object with user.id (UUID string)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create database migration and Pydantic models</name>
  <files>
    backend/migrations/007_add_upscale_batches.sql,
    backend/models/upscale.py,
    backend/tests/test_upscale_models.py
  </files>
  <behavior>
    - Test: UpscaleSettings() with no args uses defaults (resolution='2k', creativity=0, sharpen=0, grain=0, fps_boost=False, flavor='vivid')
    - Test: UpscaleSettings(creativity=50) accepts valid range value
    - Test: UpscaleSettings(creativity=101) raises ValidationError (out of range)
    - Test: UpscaleSettings(creativity=-1) raises ValidationError (out of range)
    - Test: UpscaleSettings(resolution='8k') raises ValidationError (not in Literal)
    - Test: UpscaleSettings(flavor='cinematic') raises ValidationError (not in Literal)
    - Test: CreateBatchPayload() with no args creates default UpscaleSettings
    - Test: CreateBatchPayload(settings=UpscaleSettings(resolution='4k')) accepts custom settings
    - Test: AddVideoPayload requires input_filename and input_storage_url
    - Test: BatchStatus and VideoStatus Literal types accept valid values
    - Test: BatchResponse, UpscaleBatch, UpscaleVideo, BatchDetailResponse can be instantiated with valid data
  </behavior>
  <action>
    **Step 1: Write tests first** in `backend/tests/test_upscale_models.py`:
    - Test all UpscaleSettings defaults match SETT-02 (2k, 0, 0, 0, False, vivid)
    - Test validation ranges for creativity (0-100), sharpen (0-100), grain (0-100)
    - Test Literal validation for resolution ('1k', '2k', '4k') and flavor ('vivid', 'natural')
    - Test CreateBatchPayload defaults
    - Test AddVideoPayload requires fields
    - Test response model instantiation

    **Step 2: Run tests (must fail -- RED)**

    **Step 3: Create `backend/models/upscale.py`** with these models:
    - `UpscaleSettings(BaseModel)`: resolution (Literal['1k','2k','4k'], default='2k'), creativity (int, ge=0, le=100, default=0), sharpen (int, ge=0, le=100, default=0), grain (int, ge=0, le=100, default=0), fps_boost (bool, default=False), flavor (Literal['vivid','natural'], default='vivid')
    - `BatchStatus = Literal['pending', 'processing', 'completed', 'failed', 'paused', 'cancelled']`
    - `VideoStatus = Literal['pending', 'processing', 'completed', 'failed', 'paused']`
    - `CreateBatchPayload(BaseModel)`: settings (UpscaleSettings, default_factory=UpscaleSettings), project_id (Optional[str]=None)
    - `AddVideoPayload(BaseModel)`: input_filename (str), input_storage_url (str), input_file_size (Optional[int]=None), duration_seconds (Optional[float]=None), width (Optional[int]=None), height (Optional[int]=None)
    - `BatchResponse(BaseModel)`: success (bool), batch_id (Optional[str]=None), status (Optional[BatchStatus]=None), error (Optional[str]=None)
    - `UpscaleVideo(BaseModel)`: id (str), batch_id (str), status (VideoStatus), queue_position (int), input_filename (str), input_storage_url (str), freepik_task_id (Optional[str]=None), output_storage_url (Optional[str]=None), error_message (Optional[str]=None), created_at (datetime), started_at (Optional[datetime]=None), completed_at (Optional[datetime]=None)
    - `UpscaleBatch(BaseModel)`: id (str), user_id (str), status (BatchStatus), resolution (str), creativity (int), sharpen (int), grain (int), fps_boost (bool), flavor (str), project_id (Optional[str]=None), total_videos (int), completed_videos (int), failed_videos (int), created_at (datetime), started_at (Optional[datetime]=None), completed_at (Optional[datetime]=None), error_message (Optional[str]=None), videos (List[UpscaleVideo]=[])
    - `BatchDetailResponse(BaseModel)`: success (bool), batch (Optional[UpscaleBatch]=None), error (Optional[str]=None)

    **Step 4: Run tests (must pass -- GREEN)**

    **Step 5: Create `backend/migrations/007_add_upscale_batches.sql`**:
    - Use idempotent DDL pattern from 004_add_runpod_support.sql (`DO $$ BEGIN IF NOT EXISTS ... END $$`)
    - Create `upscale_batches` table with ALL columns (including Phase 11-12 columns like last_heartbeat, pause_reason, paused_at, supabase_upload_status, drive_upload_status -- they default to NULL and cost nothing)
    - Create `upscale_videos` table with ALL columns (including retry_count, output_storage_url, drive columns -- they default to NULL)
    - Create 6 indexes: user lookup, batch status, heartbeat (partial on processing), video batch position, video batch status, freepik task_id (partial on NOT NULL)
    - Use exact column names, types, and CHECK constraints from the research ARCHITECTURE section
    - Add `RAISE NOTICE` for logging which objects were created vs already existed
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/plataforma_b/multitalk-ui/backend && python -m pytest tests/test_upscale_models.py -x -v</automated>
  </verify>
  <done>
    - All UpscaleSettings default values match SETT-02 specification
    - Validation rejects out-of-range creativity/sharpen/grain and invalid resolution/flavor
    - Migration SQL file uses idempotent DDL pattern and includes all columns for Phases 10-12
    - All 11+ tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Freepik configuration to Settings</name>
  <files>backend/config/settings.py</files>
  <action>
    Add the following fields to the `Settings` class in `backend/config/settings.py`, placed after the RunPod configuration section with a comment header `# Freepik Video Upscaler Configuration`:

    ```python
    # Freepik Video Upscaler Configuration
    FREEPIK_API_KEY: str = ""
    FREEPIK_API_BASE_URL: str = "https://api.freepik.com/v1/ai"
    FREEPIK_POLL_INTERVAL: int = 10      # seconds between status checks
    FREEPIK_TASK_TIMEOUT: int = 600      # max seconds per video (10 min)
    ```

    Do NOT modify any existing settings. Place the new block after the `HF_TOKEN` field (after line 45 in the current file) with a blank line separator.
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/plataforma_b/multitalk-ui/backend && python -c "from config.settings import Settings; s = Settings(); assert hasattr(s, 'FREEPIK_API_KEY'); assert s.FREEPIK_API_BASE_URL == 'https://api.freepik.com/v1/ai'; assert s.FREEPIK_POLL_INTERVAL == 10; assert s.FREEPIK_TASK_TIMEOUT == 600; print('Settings OK')"</automated>
  </verify>
  <done>
    - FREEPIK_API_KEY, FREEPIK_API_BASE_URL, FREEPIK_POLL_INTERVAL, FREEPIK_TASK_TIMEOUT exist in Settings
    - Default values are empty string, the Freepik base URL, 10 seconds, and 600 seconds respectively
    - Existing settings are unchanged
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_upscale_models.py -x -v` -- all model tests pass
- `python -c "from config.settings import Settings; ..."` -- settings load without error
- `python -c "from models.upscale import UpscaleSettings; s = UpscaleSettings(); print(s.model_dump())"` -- prints correct defaults
- Migration file exists at `backend/migrations/007_add_upscale_batches.sql` and contains both CREATE TABLE statements
</verification>

<success_criteria>
1. UpscaleSettings defaults match SETT-02: resolution='2k', creativity=0, sharpen=0, grain=0, fps_boost=False, flavor='vivid'
2. Pydantic validation rejects invalid ranges and Literal values
3. Migration SQL is idempotent and includes all columns for Phases 10-12
4. FREEPIK_API_KEY and related config are in Settings
5. All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`
</output>
