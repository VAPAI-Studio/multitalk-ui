---
phase: 10-foundation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - backend/services/freepik_service.py
  - backend/services/upscale_job_service.py
  - backend/tests/test_freepik_service.py
  - backend/tests/test_upscale_job_service.py
  - backend/tests/conftest.py
autonomous: true
requirements: [QUEU-01, INFR-02]

must_haves:
  truths:
    - "FreepikUpscalerService can submit a video URL and receive a task_id"
    - "FreepikUpscalerService can poll a task_id and get status/output_url"
    - "FreepikUpscalerService handles API errors gracefully (timeout, HTTP errors, missing API key)"
    - "UpscaleJobService can create batches, add videos, query batches by user, and update statuses"
    - "UpscaleJobService can find batches stuck in processing status for startup recovery"
  artifacts:
    - path: "backend/services/freepik_service.py"
      provides: "Freepik Video Upscaler API wrapper"
      exports: ["FreepikUpscalerService"]
      min_lines: 80
    - path: "backend/services/upscale_job_service.py"
      provides: "CRUD operations for upscale_batches and upscale_videos"
      exports: ["UpscaleJobService"]
      min_lines: 100
    - path: "backend/tests/test_freepik_service.py"
      provides: "Unit tests for FreepikUpscalerService with mocked httpx"
      min_lines: 80
    - path: "backend/tests/test_upscale_job_service.py"
      provides: "Unit tests for UpscaleJobService with mocked Supabase"
      min_lines: 80
  key_links:
    - from: "backend/services/freepik_service.py"
      to: "backend/config/settings.py"
      via: "Reads FREEPIK_API_KEY and FREEPIK_API_BASE_URL from settings"
      pattern: "settings\\.FREEPIK_API_KEY"
    - from: "backend/services/upscale_job_service.py"
      to: "backend/core/supabase.py"
      via: "Uses get_supabase() for database operations"
      pattern: "get_supabase"
---

<objective>
Build the two core services: FreepikUpscalerService (API wrapper for video upscaling) and UpscaleJobService (database CRUD for batches and videos).

Purpose: These services encapsulate all external API communication and database operations, keeping the API layer thin. The Freepik service follows the established RunPod service pattern. The job service follows the VideoJobService pattern.
Output: Two service files with full test coverage via mocked dependencies.
</objective>

<execution_context>
@/Users/yvesfogel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yvesfogel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-01-SUMMARY.md

@backend/services/runpod_service.py
@backend/services/video_job_service.py
@backend/config/settings.py
@backend/core/supabase.py
@backend/tests/conftest.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/config/settings.py (after Plan 01 adds Freepik fields):
```python
class Settings(BaseSettings):
    FREEPIK_API_KEY: str = ""
    FREEPIK_API_BASE_URL: str = "https://api.freepik.com/v1/ai"
    FREEPIK_POLL_INTERVAL: int = 10
    FREEPIK_TASK_TIMEOUT: int = 600
```

From backend/services/runpod_service.py (pattern to follow):
```python
class RunPodService:
    async def submit_workflow(...) -> Tuple[bool, Optional[str], Optional[str]]:
        # Returns (success, job_id, error_message)
    async def check_job_status(...) -> Tuple[bool, Optional[Dict], Optional[str]]:
        # Returns (success, status_data, error_message)
```

From backend/services/video_job_service.py (pattern to follow):
```python
class VideoJobService:
    def __init__(self, supabase: Optional[Client] = None):
        self.supabase = supabase or get_supabase()
    # Uses self.supabase.table("table_name").select/insert/update/delete
```

From backend/core/supabase.py:
```python
def get_supabase() -> Client:
    # Returns Supabase client singleton
```

From backend/models/upscale.py (created by Plan 01):
```python
class UpscaleSettings(BaseModel):
    resolution: Literal['1k', '2k', '4k'] = '2k'
    creativity: int = Field(default=0, ge=0, le=100)
    sharpen: int = Field(default=0, ge=0, le=100)
    grain: int = Field(default=0, ge=0, le=100)
    fps_boost: bool = False
    flavor: Literal['vivid', 'natural'] = 'vivid'

BatchStatus = Literal['pending', 'processing', 'completed', 'failed', 'paused', 'cancelled']
VideoStatus = Literal['pending', 'processing', 'completed', 'failed', 'paused']
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FreepikUpscalerService with tests</name>
  <files>
    backend/services/freepik_service.py,
    backend/tests/test_freepik_service.py,
    backend/tests/conftest.py
  </files>
  <behavior>
    - Test: submit_task returns (True, task_id, None) on successful 200 response with task_id in data
    - Test: submit_task returns (False, None, error_msg) when FREEPIK_API_KEY is empty
    - Test: submit_task returns (False, None, error_msg) on HTTP 400/422/500 error
    - Test: submit_task returns (False, None, error_msg) on timeout
    - Test: submit_task maps resolution '2k' to '1440p' in payload
    - Test: check_task_status returns ('COMPLETED', output_url, None) when task is done
    - Test: check_task_status returns ('IN_PROGRESS', None, None) when task is processing
    - Test: check_task_status returns ('FAILED', None, error_msg) when task failed
    - Test: check_task_status returns ('ERROR', None, error_msg) on HTTP error
    - Test: poll_until_complete returns terminal state within timeout
    - Test: poll_until_complete returns ('TIMEOUT', None, error_msg) when exceeding timeout
  </behavior>
  <action>
    **Step 1: Add Freepik mock fixtures to `backend/tests/conftest.py`:**
    - Add `mock_freepik_settings` fixture that patches `config.settings.settings` with FREEPIK_API_KEY='test-key' and FREEPIK_API_BASE_URL='https://api.freepik.com/v1/ai'
    - Add `freepik_service` fixture that returns a FreepikUpscalerService instance (uses the patched settings)

    **Step 2: Write tests in `backend/tests/test_freepik_service.py`:**
    - Use `unittest.mock.patch` on `httpx.AsyncClient` to mock HTTP calls
    - Mock responses with proper `response.json()` return values following the Freepik API contract: `{"data": {"task_id": "abc123", "status": "COMPLETED", "generated": ["https://..."]}}`
    - Test all success, error, and edge cases listed in behavior section
    - For poll_until_complete tests, use `unittest.mock.patch('asyncio.sleep')` to avoid real delays

    **Step 3: Run tests (must fail -- RED)**

    **Step 4: Create `backend/services/freepik_service.py`:**
    - Follow the RunPod service pattern exactly (httpx.AsyncClient, tuple returns, settings-based config)
    - Constructor reads `settings.FREEPIK_API_KEY`, `settings.FREEPIK_API_BASE_URL`, `settings.FREEPIK_POLL_INTERVAL`, `settings.FREEPIK_TASK_TIMEOUT`
    - `submit_task(video_url, resolution, creativity, sharpen, grain, fps_boost, flavor) -> Tuple[bool, Optional[str], Optional[str]]`
      - Maps resolution: {'1k': '1080p', '2k': '1440p', '4k': '2160p'}
      - POST to `{base_url}/video-upscaler` with `x-freepik-api-key` header
      - Returns (True, task_id, None) or (False, None, error)
    - `check_task_status(task_id) -> Tuple[str, Optional[str], Optional[str]]`
      - GET `{base_url}/video-upscaler/{task_id}` with `x-freepik-api-key` header
      - Returns (status, output_url_if_completed, error_if_failed)
    - `poll_until_complete(task_id, timeout=None) -> Tuple[str, Optional[str], Optional[str]]`
      - Polls check_task_status with exponential backoff (start at FREEPIK_POLL_INTERVAL, cap at 30s)
      - Returns terminal state or ('TIMEOUT', None, error) if timeout exceeded
      - Uses settings.FREEPIK_TASK_TIMEOUT as default timeout
    - All methods have try/except for httpx.HTTPStatusError, httpx.TimeoutException, and generic Exception

    **Step 5: Run tests (must pass -- GREEN)**
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/plataforma_b/multitalk-ui/backend && python -m pytest tests/test_freepik_service.py -x -v</automated>
  </verify>
  <done>
    - FreepikUpscalerService can submit, poll, and handle errors for Freepik API
    - Resolution mapping (1k/2k/4k to 1080p/1440p/2160p) is correct
    - All error paths return (False/ERROR, None, descriptive_error_message)
    - All 11+ tests pass with mocked httpx
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build UpscaleJobService with tests</name>
  <files>
    backend/services/upscale_job_service.py,
    backend/tests/test_upscale_job_service.py,
    backend/tests/conftest.py
  </files>
  <behavior>
    - Test: create_batch inserts into upscale_batches with settings and user_id, returns batch data
    - Test: add_video_to_batch inserts into upscale_videos with correct queue_position and increments total_videos
    - Test: get_batch returns batch with nested videos list, ordered by queue_position
    - Test: get_batch returns None for non-existent batch_id
    - Test: list_user_batches returns batches for given user_id ordered by created_at desc
    - Test: update_batch_status updates status and sets started_at when transitioning to 'processing'
    - Test: update_video_status updates video status and sets started_at/completed_at appropriately
    - Test: increment_completed_count increments completed_videos on batch
    - Test: increment_failed_count increments failed_videos on batch
    - Test: get_next_pending_video returns lowest queue_position video with status 'pending'
    - Test: get_batches_by_status returns all batches matching given status (for startup recovery)
    - Test: update_batch_heartbeat updates last_heartbeat timestamp
    - Test: fail_current_processing_video finds video with status 'processing' in batch and marks it 'failed'
  </behavior>
  <action>
    **Step 1: Add Supabase mock fixture to `backend/tests/conftest.py`** (if not already added in Task 1):
    - Add `mock_supabase` fixture that creates a MagicMock Supabase client with chainable `.table().select().eq().execute()` pattern
    - Add `upscale_job_service` fixture that returns UpscaleJobService(supabase=mock_supabase)

    **Step 2: Write tests in `backend/tests/test_upscale_job_service.py`:**
    - Mock the Supabase client method chains (`.table("upscale_batches").insert({...}).execute()`)
    - Each test verifies the correct table name, query method, and parameters
    - Test return value parsing (service should return parsed dicts/objects, not raw Supabase responses)

    **Step 3: Run tests (must fail -- RED)**

    **Step 4: Create `backend/services/upscale_job_service.py`:**
    - Follow VideoJobService pattern: constructor accepts optional Supabase client, falls back to get_supabase()
    - Methods (all async):
      - `create_batch(user_id: str, settings: UpscaleSettings, project_id: Optional[str] = None) -> Tuple[bool, Optional[dict], Optional[str]]`
      - `add_video_to_batch(batch_id: str, user_id: str, input_filename: str, input_storage_url: str, queue_position: int, input_file_size: Optional[int] = None, duration_seconds: Optional[float] = None, width: Optional[int] = None, height: Optional[int] = None) -> Tuple[bool, Optional[dict], Optional[str]]`
      - `get_batch(batch_id: str, user_id: str) -> Optional[dict]` -- returns batch with videos list joined
      - `list_user_batches(user_id: str, limit: int = 20) -> List[dict]`
      - `update_batch_status(batch_id: str, status: str, error_message: Optional[str] = None) -> bool` -- also sets started_at when status='processing', completed_at when status in ('completed','failed')
      - `update_video_status(video_id: str, status: str, freepik_task_id: Optional[str] = None, output_url: Optional[str] = None, error_message: Optional[str] = None) -> bool` -- sets started_at when 'processing', completed_at when terminal
      - `increment_completed_count(batch_id: str) -> bool`
      - `increment_failed_count(batch_id: str) -> bool`
      - `get_next_pending_video(batch_id: str) -> Optional[dict]` -- select from upscale_videos where batch_id and status='pending' order by queue_position limit 1
      - `get_batches_by_status(status: str) -> List[dict]` -- for startup recovery
      - `update_batch_heartbeat(batch_id: str) -> bool` -- sets last_heartbeat to now()
      - `fail_current_processing_video(batch_id: str, error_message: str) -> bool` -- find video with status='processing' in batch and mark failed

    All methods use try/except and return tuple (bool, data, error) for write ops or Optional for reads.

    **Step 5: Run tests (must pass -- GREEN)**
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/plataforma_b/multitalk-ui/backend && python -m pytest tests/test_upscale_job_service.py -x -v</automated>
  </verify>
  <done>
    - UpscaleJobService performs all CRUD operations for upscale_batches and upscale_videos
    - Status transitions correctly set timestamp fields (started_at, completed_at)
    - get_next_pending_video returns the lowest queue_position pending video
    - get_batches_by_status works for startup recovery queries
    - All 13+ tests pass with mocked Supabase client
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_freepik_service.py tests/test_upscale_job_service.py -x -v` -- all tests pass
- `python -c "from services.freepik_service import FreepikUpscalerService; print('Import OK')"` -- service imports cleanly
- `python -c "from services.upscale_job_service import UpscaleJobService; print('Import OK')"` -- service imports cleanly
</verification>

<success_criteria>
1. FreepikUpscalerService can submit, poll, and handle all error cases for the Freepik video upscaler API
2. UpscaleJobService provides complete CRUD for batches and videos with proper status transitions
3. Both services follow established codebase patterns (tuple returns, httpx, Supabase client)
4. All tests pass with mocked external dependencies (no real API calls or database)
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-02-SUMMARY.md`
</output>
