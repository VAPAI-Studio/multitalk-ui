---
phase: 02-network-volume-file-browser
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified: []
autonomous: false
requirements: [VOL-01, VOL-02, VOL-03, VOL-04, VOL-05]

must_haves:
  truths:
    - "All requirements VOL-01 through VOL-05 are verified working end-to-end"
    - "Admin can successfully browse RunPod network volume files"
    - "File browser handles large directories without crashing"
    - "Navigation via breadcrumb and tree is intuitive and functional"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 2: Network Volume File Browser.

Purpose: Verify the complete file browser implementation works correctly before proceeding to Phase 3.

Output:
- Human approval confirming all features work as expected
- OR feedback on issues that need fixing
</objective>

<execution_context>
@/Users/yvesfogel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yvesfogel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-network-volume-file-browser/02-RESEARCH.md
@.planning/phases/02-network-volume-file-browser/02-01-SUMMARY.md
@.planning/phases/02-network-volume-file-browser/02-02-SUMMARY.md
@.planning/phases/02-network-volume-file-browser/02-03-SUMMARY.md
</context>

<tasks>

<checkpoint type="human-verify" gate="blocking">
  <what-built>
    Complete file browser for RunPod network volume with:
    - Hierarchical tree view with lazy loading
    - Breadcrumb navigation for path traversal
    - Refresh functionality
    - S3 connectivity health check
  </what-built>
  <how-to-verify>
    1. Navigate to Infrastructure page (http://localhost:5173 → Infrastructure)

    2. Verify S3 configuration:
       - If you see error: "Failed to load network volume contents"
       - Check backend .env has all 5 S3 variables configured
       - Check health endpoint shows s3_connected: true

    3. Test file browser features:
       ✓ Root directory loads automatically
       ✓ Files show: name, size (human-readable), last modified date
       ✓ Folders show: name, folder icon
       ✓ Click folder → expands to show contents (lazy-loaded)
       ✓ Click folder again → collapses
       ✓ Nested folders indent progressively
       ✓ Loading spinner shows during fetch
       ✓ Empty folders show "Empty folder" message

    4. Test breadcrumb navigation:
       ✓ Breadcrumb shows "🏠 Root" initially
       ✓ Click folder → breadcrumb updates with path
       ✓ Click breadcrumb segment → navigates to that level
       ✓ Current location highlighted in breadcrumb
       ✓ Long paths scroll horizontally

    5. Test refresh button:
       ✓ Click Refresh → directory reloads
       ✓ Spinning icon appears during reload
       ✓ Current path preserved after refresh

    6. Test error handling:
       ✓ Invalid credentials → clear error message
       ✓ Network error → retry button works
       ✓ Empty folder → appropriate message

    7. Test responsiveness:
       ✓ Mobile view hides last modified dates
       ✓ Breadcrumb scrollable on small screens
       ✓ Tree navigation works on touch devices

    Expected behavior: Full-featured file browser that loads quickly, navigates smoothly, and handles errors gracefully.

    VOL-01 ✓: Admin sees hierarchical tree of files and folders
    VOL-02 ✓: File browser displays name, size (human-readable), last modified date
    VOL-03 ✓: Admin can expand/collapse folders without page reload
    VOL-04 ✓: File browser handles large directories with pagination (200 items per page)
    VOL-05 ✓: Admin can navigate to any path level via breadcrumb
  </how-to-verify>
  <resume-signal>
    Type "approved" if file browser works as expected.
    OR describe any issues found (e.g., "breadcrumb not updating", "S3 connection fails", "styling issues").
  </resume-signal>
</checkpoint>

</tasks>

<output>
After checkpoint approval, create `.planning/phases/02-network-volume-file-browser/02-04-SUMMARY.md` using the standard summary template.
</output>
